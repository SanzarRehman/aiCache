server:
  port: 8080

spring:
  ai:
    vectorstore:
      chroma:
        client:
          host: http://localhost
          port: 8000

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

